import React from 'react'

const Modal = () => {
    // console.log(open)
  return (
    // <div onClick={onClose}
    // className={`fixed inset-0 flex justify-center items-center transition-colors ${open ? "visisble bg-gray-500/20" : "invisible"}`}
    // >
    //     <div onClick={e => e.stopPropagation()}
    //     className={`bg-white rounded-lg shadow p-6 max-w-md transition-all ${open ? "scale-100 opacity-100" : "scale-11- opacity-0"}`}    
    //     >

    //     </div>
    // </div>
            <div>

                {/* {children} */}
                {/* Modal toggle */}
                {/* <button data-modal-target="default-modal" data-modal-toggle="default-modal"  type="button"
                // onClick={onClose}
                className='bg-custom-red text-custom-rose50 w-full p-4 my-8 rounded-4xl text-lg font-bold hover:bg-custom-red/80'
                >
                    Start another order
                </button> */}
            </div>
  )
}

export default Modal